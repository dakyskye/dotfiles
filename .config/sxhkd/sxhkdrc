# reload the sxhkd config
super + Escape
	pkill -USR1 -x sxhkd

# launch termite
super + Return
	termite

# launch rofi
super + d
	rofi -modi drun -show drun

# launch neofetch
Print
	flameshot gui

# change the wallpaper
super + shift + w
	go run $HOME/.config/scripts/wallpapers.go
